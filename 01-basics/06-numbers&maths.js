const score = 10.6589

console.log([
    score,
    score.toString().length,
    score.toFixed(2),
    score.toPrecision(2),
    score.toPrecision(3)
])
let hundreds = 10000000000
console.log([hundreds , hundreds.toLocaleString("en-IN")])

// *********************** MATHS *********************
console.log( Math)